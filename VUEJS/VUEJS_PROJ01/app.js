/**
 * Criando a instância do Vue
 * - atributo el indica qual trecho de html que queremos controlar;
 * - atributo data onde colocaremos os dados que serão utilizados
 * no meu template html
 * 
 * Veremos mais para a frente que dentro de Componente (data) precisa ser uma
 * função e não um objeto como definimos agora.
 * 
 */
new Vue({
    el: '#app',
    data: {

    }
})